import UserHeader from "@/components/islands/user/UserHeader";
import { FC } from "react";

const User: FC = () => {
    return (<>
        <UserHeader />
    </>)
}

User.displayName = "User";
export default User;